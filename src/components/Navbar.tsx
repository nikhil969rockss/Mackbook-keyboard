import React from "react";

const Navbar = () => {
  return (
    <div className="w-full">
      <h1 className="bg-gradient-to-b from-gray-300 to-white/30 bg-clip-text text-center text-[max(3vw,24px)] font-[700] tracking-tighter text-transparent selection:bg-gray-900 selection:text-white">
        MacBookPro KeyBorad
      </h1>
    </div>
  );
};

export default Navbar;
